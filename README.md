# hermes 

Random Internal library across a bunch of things .

`hermes.Deref` and `hermes.Ref` to take pointers and values of anything
`config.Export` allows you export any given struct into either yml,json, env file
`GetIP` gets the user IP looking through cf and others
